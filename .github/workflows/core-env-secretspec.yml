name: core-env-secretspec CI
on:
  push:
    branches: [main]
    paths:
      - "packages/core-env-secretspec/**"
      - ".github/workflows/core-env-secretspec.yml"
      - ".github/workflows/module.yml"

permissions:
  id-token: write # Required for OIDC
  contents: read

jobs:
  module-flow:
    uses: ./.github/workflows/module.yml
    with:
      package-path: packages/core-env-secretspec
