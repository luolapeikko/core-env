name: core-env-azurekv CI
on:
  push:
    branches: [main]
    paths:
      - "packages/core-env-azurekv/**"
      - ".github/workflows/core-env-azurekv.yml"
      - ".github/workflows/module.yml"

permissions:
  id-token: write # Required for OIDC
  contents: read

jobs:
  module-flow:
    uses: ./.github/workflows/module.yml
    with:
      package-path: packages/core-env-azurekv
