name: core-env CI
on:
  push:
    branches: [main]
    paths:
      - "packages/core-env/**"
      - ".github/workflows/core-env.yml"
      - ".github/workflows/module.yml"

jobs:
  module-flow:
    uses: ./.github/workflows/module.yml
    with:
      package-path: packages/core-env
